<launch>

	<arg name="respawn" default="false"/>

	<!-- RVIZ -->
	<node name="$(anon rviz)" pkg="rviz" type="rviz"
	 respawn="$(arg respawn)" output="screen" args="-d $(find maqui_gazebo)/params/rviz_sim/popper_virtual.rviz"/>

</launch>
